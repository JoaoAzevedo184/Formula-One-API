PORT=3333

# Pooled — usada pela aplicação (tem "-pooler" no hostname)
DATABASE_URL="postgresql://user:senha@ep-xxxx-pooler.regiao.aws.neon.tech/formula_one?sslmode=require&connect_timeout=15"

# Direta — usada pelo CLI do Prisma (sem "-pooler")
DIRECT_URL="postgresql://user:senha@ep-xxxx.regiao.aws.neon.tech/formula_one?sslmode=require"